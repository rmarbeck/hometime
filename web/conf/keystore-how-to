1 ) Télécharger le fichier GIAG2.crt sur https://pki.google.com/

si ca ne marche pas, récupérer le certificat avec la commande echo -n | openssl s_client -connect smtp.gmail.com:465 | sed -ne '/-BEGIN CERTIFICATE-/,/-END CERTIFICATE-/p' > /tmp/cert.pem

2) L'importer dans le fichier 
keytool -J-Duser.language=en -import -alias smtp.gmail.com -keystore conf/keystore -file conf/GIAG2.crt

ou bien cert.pem

ou mise à jour  

keytool -delete -alias smtp.gmail.com -keystore conf/keystore